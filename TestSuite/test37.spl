int f(int x) {
	if (x <= 0)
		return 1;

	return x * g(x - 1);
}

int g(int x) {
	if (x <= 0)
		return 1;

	return x * f(x - 1);
}

/* Strange faculty function
 * - shows order of functions does not matter
 */
void main() {
	print(f(4));
}
