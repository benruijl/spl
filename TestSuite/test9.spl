bool isEmpty([t] list)
{
	if (list == [])return true;
	return false;
}	
int size ([t]  list)
{
	if(isEmpty (list))return 0;
	else return 1 + size(tail(list));
}

int main ()
{
	[int] list = [];
	int n = 5;
	int	i = 0;

	print (size(list));
	
	while (i < 5)
	{
		list = i:list;
	}
	
	print(size(i));
	
	return 0;
}
